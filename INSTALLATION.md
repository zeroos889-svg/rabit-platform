# 📦 دليل التثبيت الشامل - رابِط

> **دليل خطوة بخطوة لتثبيت وتشغيل منصة رابِط للموارد البشرية**

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من وجود:

### البرامج المطلوبة
- **Node.js** 18.0 أو أعلى ([تحميل](https://nodejs.org/))
- **npm** أو **pnpm** (يأتي مع Node.js)
- **Git** ([تحميل](https://git-scm.com/))
- **MySQL** 8.0 أو **TiDB** ([تحميل](https://www.mysql.com/downloads/))

### الحسابات المطلوبة
- حساب **Manus** للمصادقة
- حساب **Moyasar** أو **Tap Payments** للدفع (اختياري)
- حساب **Google Cloud** لخدمات الخرائط (اختياري)

---

## 🚀 خطوات التثبيت

### الخطوة 1: استنساخ المستودع

```bash
# استنساخ المشروع
git clone https://github.com/yourusername/rabit-hr-platform.git

# الدخول إلى مجلد المشروع
cd rabit-hr-platform
```

### الخطوة 2: تثبيت الحزم

اختر إحدى الطرق:

#### باستخدام pnpm (مُفضل)
```bash
# تثبيت pnpm إذا لم يكن مثبتاً
npm install -g pnpm

# تثبيت الحزم
pnpm install
```

#### باستخدام npm
```bash
npm install
```

#### باستخدام yarn
```bash
yarn install
```

### الخطوة 3: إعداد قاعدة البيانات

#### أ. إنشاء قاعدة البيانات

```bash
# تسجيل الدخول إلى MySQL
mysql -u root -p

# إنشاء قاعدة البيانات
CREATE DATABASE rabit_hr_platform;
CREATE USER 'rabit_user'@'localhost' IDENTIFIED BY 'secure_password_here';
GRANT ALL PRIVILEGES ON rabit_hr_platform.* TO 'rabit_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

#### ب. إعداد متغيرات البيئة

```bash
# نسخ ملف البيئة النموذجي
cp .env.example .env

# تحرير الملف وإضافة بيانات قاعدة البيانات
nano .env
```

#### ج. تشغيل الهجرات

```bash
# تطبيق الهجرات على قاعدة البيانات
pnpm db:push

# أو
npm run db:push
```

### الخطوة 4: إعداد متغيرات البيئة

تحرير ملف `.env` وإضافة المتغيرات التالية:

```env
# قاعدة البيانات
DATABASE_URL=mysql://rabit_user:secure_password_here@localhost:3306/rabit_hr_platform

# المصادقة
JWT_SECRET=your-very-secure-secret-key-here-minimum-32-characters
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://portal.manus.im

# Manus OAuth
VITE_APP_ID=your-manus-app-id
VITE_APP_TITLE=رابِط
VITE_APP_LOGO=https://example.com/logo.png

# API الداخلي
BUILT_IN_FORGE_API_URL=https://api.manus.im
BUILT_IN_FORGE_API_KEY=your-forge-api-key
VITE_FRONTEND_FORGE_API_KEY=your-frontend-forge-key
VITE_FRONTEND_FORGE_API_URL=https://api.manus.im

# المالك
OWNER_NAME=Your Name
OWNER_OPEN_ID=your-open-id

# التحليلات
VITE_ANALYTICS_ENDPOINT=https://analytics.example.com
VITE_ANALYTICS_WEBSITE_ID=your-website-id

# الدفع (اختياري)
MOYASAR_API_KEY=your-moyasar-key
TAP_API_KEY=your-tap-key

# Google Maps (اختياري)
GOOGLE_MAPS_API_KEY=your-google-maps-key
```

### الخطوة 5: تشغيل المشروع

```bash
# تشغيل بيئة التطوير
pnpm dev

# أو
npm run dev
```

سيظهر الرابط:
```
Server running on http://localhost:3000
```

---

## ✅ التحقق من التثبيت

### 1. التحقق من الخادم

```bash
# في نافذة جديدة
curl http://localhost:3000
```

يجب أن تحصل على صفحة HTML.

### 2. التحقق من قاعدة البيانات

```bash
# التحقق من الاتصال
mysql -u rabit_user -p -h localhost rabit_hr_platform -e "SHOW TABLES;"
```

يجب أن ترى قائمة الجداول.

### 3. فتح الموقع في المتصفح

```
http://localhost:3000
```

يجب أن ترى الصفحة الرئيسية.

---

## 🧪 الاختبار الأولي

### 1. اختبار التسجيل

1. انتقل إلى `/signup`
2. اختر نوع الحساب (موظف، مستشار، شركة)
3. أكمل النموذج
4. تحقق من البريد الإلكتروني

### 2. اختبار الدخول

1. انتقل إلى `/login`
2. أدخل بيانات الاعتماد
3. يجب أن يتم توجيهك إلى لوحة التحكم

### 3. اختبار الأدوات

1. انتقل إلى `/tools`
2. جرب حاسبة الراتب
3. جرب حاسبة الإجازات

---

## 🔧 الأوامر المتاحة

```bash
# تشغيل بيئة التطوير
pnpm dev

# بناء المشروع للإنتاج
pnpm build

# تشغيل الخادم في الإنتاج
pnpm start

# تشغيل الاختبارات
pnpm test

# فحص الأخطاء
pnpm lint

# إصلاح الأخطاء تلقائياً
pnpm lint:fix

# تطبيق هجرات قاعدة البيانات
pnpm db:push

# إنشاء هجرة جديدة
pnpm db:generate

# فتح واجهة قاعدة البيانات
pnpm db:studio
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: خطأ الاتصال بقاعدة البيانات

**الأعراض:**
```
Error: connect ECONNREFUSED 127.0.0.1:3306
```

**الحل:**
```bash
# تحقق من أن MySQL يعمل
sudo service mysql status

# إذا لم يكن يعمل، شغله
sudo service mysql start

# تحقق من بيانات الاتصال في .env
cat .env | grep DATABASE_URL
```

### المشكلة 2: خطأ المنافذ المشغولة

**الأعراض:**
```
Error: listen EADDRINUSE: address already in use :::3000
```

**الحل:**
```bash
# ابحث عن العملية التي تستخدم المنفذ 3000
lsof -i :3000

# اقتل العملية
kill -9 <PID>

# أو استخدم منفذ مختلف
PORT=3001 pnpm dev
```

### المشكلة 3: أخطاء في الحزم

**الأعراض:**
```
npm ERR! code ERESOLVE
```

**الحل:**
```bash
# حذف node_modules و lockfile
rm -rf node_modules pnpm-lock.yaml

# إعادة التثبيت
pnpm install
```

### المشكلة 4: أخطاء TypeScript

**الأعراض:**
```
error TS2307: Cannot find module
```

**الحل:**
```bash
# تحديث المسارات في tsconfig.json
# أو إعادة تشغيل السيرفر
pnpm dev
```

### المشكلة 5: مشاكل الترجمة

**الأعراض:**
```
Translation key not found
```

**الحل:**
```bash
# تحقق من ملف الترجمة
cat client/src/lib/i18n.ts | grep "your-key"

# أضف المفتاح المفقود
```

---

## 📊 هيكل الملفات بعد التثبيت

```
rabit-hr-platform/
├── node_modules/              # الحزم المثبتة
├── client/                    # الواجهة الأمامية
│   ├── src/
│   │   ├── pages/            # جميع الصفحات
│   │   ├── components/       # المكونات
│   │   ├── hooks/            # Hooks المخصصة
│   │   ├── lib/              # المكتبات
│   │   └── main.tsx
│   └── public/               # الملفات الثابتة
├── server/                    # الخادم الخلفي
│   ├── routers.ts            # tRPC routers
│   ├── db.ts                 # دوال قاعدة البيانات
│   └── _core/                # ملفات النظام
├── drizzle/                   # قاعدة البيانات
│   ├── schema.ts             # تعريف الجداول
│   └── migrations/           # الهجرات
├── .env                       # متغيرات البيئة
├── package.json              # حزم المشروع
├── tsconfig.json             # إعداد TypeScript
├── vite.config.ts            # إعداد Vite
└── README.md                 # التوثيق
```

---

## 🚀 الخطوات التالية

بعد التثبيت الناجح:

1. **اقرأ التوثيق**
   - اقرأ `README.md` للحصول على نظرة عامة
   - اقرأ `TEST_USERS.md` لبيانات الاختبار

2. **استكشف الكود**
   - ابدأ بـ `client/src/App.tsx` لفهم الروتينج
   - ابدأ بـ `server/routers.ts` لفهم الـ APIs

3. **اختبر الميزات**
   - استخدم `TEST_USERS.md` للاختبار
   - جرب جميع الصفحات والميزات

4. **قم بالتخصيص**
   - عدّل الألوان والتصاميم
   - أضف ميزات جديدة
   - ربط الدفع الفعلي

---

## 📞 الدعم والمساعدة

إذا واجهت مشاكل:

1. **تحقق من السجلات**
   ```bash
   # عرض سجلات الخادم
   pnpm dev
   ```

2. **ابحث عن الحل**
   - ابحث في `docs/` عن الحل
   - ابحث في GitHub Issues

3. **اطلب المساعدة**
   - البريد الإلكتروني: support@rabit.com
   - الدردشة: https://rabit.com/chat

---

## ✨ نصائح مفيدة

### 1. استخدام Drizzle Studio

```bash
# فتح واجهة قاعدة البيانات
pnpm db:studio
```

### 2. تتبع التغييرات

```bash
# عرض حالة Git
git status

# عرض السجل
git log --oneline
```

### 3. تنظيف المشروع

```bash
# حذف الملفات المؤقتة
rm -rf .vite dist build

# حذف node_modules
rm -rf node_modules
```

### 4. تحديث الحزم

```bash
# تحديث جميع الحزم
pnpm update

# تحديث حزمة معينة
pnpm update package-name
```

---

## 🎉 تم التثبيت بنجاح!

الآن يمكنك:
- ✅ تشغيل الموقع محلياً
- ✅ تطوير ميزات جديدة
- ✅ اختبار التطبيق
- ✅ نشر على الإنتاج

---

**آخر تحديث:** 2025-11-02  
**الإصدار:** 1.0.0

صُنع بـ ❤️ بواسطة فريق رابِط
